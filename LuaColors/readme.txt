LUACOLORS README

LuaColors is a python script which reads in a text file with the following format:

r g b index colorname

From the text file "colors_list", the python script creates a lua file with a colors table.
To add colors, just added the color values in the format specified above and run the script.